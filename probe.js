const { Client } = require('pg'); const client = new Client({ user: 'postgres', host: 'localhost', port: 5432, database: 'postgres' }); client.connect().then(() => client.query('SELECT datname FROM pg_database;')).then(res => { console.log('Databases:', res.rows.map(r => r.datname)); client.end(); }).catch(e => { console.error('Connection failed:', e.message); process.exit(1); });